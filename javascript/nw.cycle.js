/*name:		nw.cycle.jsfunction:	like the Max cycle object, only for any input typecreated:	2005.10.10 --- Nathan Woleklast mod:	2006.05.02NOTE: cycle now supports any input type, so this is only included for legacy patches© 2005, lowkey digital studio*///globalif (jsarguments.length>1){	outlets = jsarguments[1];}else{	outlets = 1;}inlets = 1;var curr_outlet = 0;setinletassist(-1,in_describe);setoutletassist(-1,out_describe);function anything(){	var outmess = arrayfromargs(messagename,arguments);		if (curr_outlet >= outlets) { curr_outlet = 0; }	outlet(curr_outlet,outmess);	curr_outlet = curr_outlet + 1;		}function in_describe(num){	assist("input for cycling");}function out_describe(num){	assist("outlet num " + num);}